// const signalProcessor = require("../services/signalProcessor");

// const analyzeText = (req, res) => {
//   const { text } = req.body;
//   if (!text) return res.status(400).json({ error: "Missing input text" });

//   const signal = signalProcessor.extractSignal(text);
//   res.json(signal);
// };

// module.exports = { analyzeText };
