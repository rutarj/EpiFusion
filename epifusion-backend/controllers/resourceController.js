// const resourceEstimator = require("../services/resourceEstimator");

// const getResourcesForLocation = (req, res) => {
//   const { location } = req.query;

//   if (!location) {
//     return res.status(400).json({ error: "Missing location parameter" });
//   }

//   const result = resourceEstimator.estimate(location);
//   res.json(result);
// };

// module.exports = { getResourcesForLocation };
